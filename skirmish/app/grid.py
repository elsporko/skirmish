import Unit

class grid(object):

    def __init__:

    def createGrid(self):
    """
    Build the grid

    TODO: Put in a check to make sure that the caller is in the user class
    """
        grid_matrix = [][]
        for x in range (10):
            for y in range (10):
                square = Unit()
                grid_matrix.append(square)
        return grid_matrix

    def placePiece(self, piece, x, y):
    """
    Place piece on the matrix
    """
        pass
